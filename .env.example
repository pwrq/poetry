OPENROUTER_API_KEY=sk-or-your-key-here

# Fallback model used only if an agent's model field is somehow empty.
# In practice each agent has its own model set in the UI or in base.py.
DEFAULT_MODEL=anthropic/claude-3.5-haiku
